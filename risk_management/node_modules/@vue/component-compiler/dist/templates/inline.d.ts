import { AssembleSource } from "../assembler";
import { SFCCompiler } from "../compiler";
export default function (compiler: SFCCompiler, {filename, script, template, styles, scopeId}: AssembleSource): string;
